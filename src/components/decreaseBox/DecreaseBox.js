import DecreaseButton from "../decreaseButton/DecreaseButton"

const DecreaseBox = ()=>{
    return(
        <>
            <p>its a component to decrease counter</p>
            <DecreaseButton />
        </>
    )
}

export default DecreaseBox